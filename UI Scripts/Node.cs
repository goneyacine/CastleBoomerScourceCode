using UnityEngine;
public class Node 
{
    //the world position of the node
    public Vector2 position;
    //the scale of the node
    public Vector2 scale;
    public Node(Vector2 position, Vector2 scale)
    {
        this.position = position;
        this.scale = scale;
    }
    //i have bug on the die effects partical system and i need finish the rows and the grid system .
}
